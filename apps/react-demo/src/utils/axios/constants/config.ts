import { PluginAction } from "../types/plugin";

// base config
const TIME_OUT = 10 * 1000;

const PLUGIN_ACTIONS: PluginAction[] = [
  // { name: "eventTrackingRetry" }
];

export { TIME_OUT, PLUGIN_ACTIONS };
